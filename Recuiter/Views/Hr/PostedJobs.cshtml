@model IEnumerable<Recruiter.ViewModels.PostedJobVM>

@{
    ViewBag.Title = "PostedJobs";
}

<h2>PostedJobs</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
	<tr>
		<th>
			@Html.DisplayNameFor(model => model.Department)
		</th>
		<th>
			@Html.DisplayNameFor(model => model.ContractClass)
		</th>
		<th>
			@Html.DisplayNameFor(model => model.DatePosted)
		</th>
		<th>
			@Html.DisplayNameFor(model => model.Status)
		</th>

		<th></th>
	</tr>

@foreach (var item in Model) {
	<tr>
		<td>
			@Html.DisplayFor(modelItem => item.Department)
		</td>
		<td>
			@Html.DisplayFor(modelItem => item.ContractClass)
		</td>
		<td>
			@Html.DisplayFor(modelItem => item.DatePosted)
		</td>
		<td>
			@Html.DisplayFor(modelItem => item.Status)
		</td>

		<td>
			@Html.ActionLink("Edit", "Edit", new { id = item.Department }) |
			@Html.ActionLink("Details", "Details", new { id = item.Department }) |
			@Html.ActionLink("Delete", "Delete", new { id = item.Department })
		</td>
	</tr>
}

</table>
